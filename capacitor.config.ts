import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ajirahq.esaccos',
  appName: 'MV Kit',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
